#include <bits/stdc++.h>

#define F first
#define S second
#define PB push_back
#define MP make_pair
typedef long long ll;

using namespace std;

int main() {
  // speed boost; use "\n" instead of endl
  ios::sync_with_stdio(0);
  cin.tie(0);

  // reads string from input line
  // string s;
  // getline(cin, s);

  // checks if double a = b
  // if (abs(a-b) < 1e-9) {
  //  // a = b
  //}

  // while input exists
  // while (cin >> x) {
  //  // code
  //}

  // opens input/output files
  // freopen("input.txt", "r", stdin);
  // freopen("output.txt", "w", stdout);
}
